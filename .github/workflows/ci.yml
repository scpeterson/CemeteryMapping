name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: Lint, build, and test
    runs-on: ubuntu-latest
    env:
      APP_ENV: test

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Start PostGIS database
        run: |
          for attempt in 1 2 3; do
            npm run db:up && exit 0
            if [ "$attempt" = "3" ]; then
              exit 1
            fi
            sleep 10
          done

      - name: Validate database changelog
        run: npm run db:validate

      - name: Test database rollback scripts
        run: npm run db:rollback:test

      - name: Apply database migrations
        run: npm run db:migrate

      - name: Load demo seed data
        run: npm run db:seed:demo

      - name: Check database migration status
        run: npm run db:status

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Lint
        run: npm run lint

      - name: Run server unit tests
        run: npm run test:server

      - name: Run database business rule tests
        run: npm run test:db-rules

      - name: Build
        run: npm run build:test

      - name: Run end-to-end tests
        run: npm run test:e2e

      - name: Stop database
        if: always()
        run: npm run db:down
