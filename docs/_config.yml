title: Cemetery Mapping Documentation
description: Architecture decisions, rebuild instructions, and data source notes for the Cemetery Mapping application.
markdown: kramdown
logo: /assets/images/site-logo.svg
google_analytics:

plugins:
  - jekyll-relative-links
  - jekyll-seo-tag

relative_links:
  enabled: true
  collections: false

defaults:
  - scope:
      path: ""
    values:
      layout: default
